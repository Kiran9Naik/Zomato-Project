name: tomato-service
on:
  push:
    branches: ["dev"]

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

  docker-build:
    needs: checkout
    runs-on: ubuntu-latest
    steps:
